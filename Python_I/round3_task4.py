from sys import argv

def wordTypes(text):
	parts = []
	parts = text.split()
	print(parts)
'''
	try:
		i = int(word)
		if int in typedict:
			typedict[int].append(i)
		else:
			typedict[int] = [i]
			
			...
			
	typedict = {}
	typedict[int] = if int in typedict:
		
		...
'''

wordTypes(argv[1])
